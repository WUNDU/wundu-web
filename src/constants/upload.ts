export const ALLOWED_UPLOAD_MIMES = ["application/pdf", "image/jpeg", "image/png"];
export const ALLOWED_UPLOAD_MIME = "application/pdf"; // kept for backward compat
export const MAX_UPLOAD_FILE_SIZE_BYTES = 20 * 1024 * 1024; // 20MB
export const MAX_UPLOAD_FILE_SIZE_MB = 20;
